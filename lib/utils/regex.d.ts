import { Regex } from "./interface";
export default class implements Regex {
    email: RegExp;
    phone: RegExp;
    cellphone: RegExp;
    postalCode: RegExp;
    password: RegExp;
    cpf: RegExp;
}
