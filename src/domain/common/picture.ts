import {staticImplements} from "../../utils/decorators";
import {StaticallyConstructable} from "../../domain/interface";

export class Picture {

    readonly id?: number;
    readonly key?: string;
    readonly url?: string;
    readonly visibility?: string;

}